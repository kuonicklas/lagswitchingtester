Controls:
-WASD for movement.
-Hold space to block packets from sending or receiving (packet switching).
-Hold P for a circle that indicates the "critical zone."
-Hold L to exhibit consistent lag.

-The Makefile is not meant for public use, but you can compile this yourself if you adjust the library paths and have SDL and ENet.